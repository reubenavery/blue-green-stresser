apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-workload
  labels:
    app: blue-green-testing
    variant: blue
spec:
  selector:
    matchLabels:
      app: blue-green-testing
      variant: blue
  template:
    metadata:
      labels:
        app: blue-green-testing
        variant: blue
    spec:
      containers:
        - name: blue-workload
          image: blue
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: blue-workload
  labels:
    app: blue-green-testing
    variant: blue
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: blue-green-testing
    variant: blue
